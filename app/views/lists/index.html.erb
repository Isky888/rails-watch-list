<h1>Your Lists</h1>
<% @lists.each do |list|  %>
<div class="card-product">
  <img src="<%=list.movies.first.poster_url%>"/>
  <div class="card-product-infos">
    <h2><%=list.name %></h2>
    <p><%= link_to 'Click for full details', list_path(list) %></p>
  </div>
</div>
<% end%>

<hr>

<%=link_to "Create a new List", new_list_path, class: "btn btn-dark" %></a>
